version: 2
updates:
  - package-ecosystem: gomod
    directory: /
    # Trigger another immediate re-check after the Go proxy starts serving v0.1.2.
    schedule:
      interval: daily
    labels:
      - automerge
