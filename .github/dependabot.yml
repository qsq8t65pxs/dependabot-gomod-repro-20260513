version: 2
updates:
  - package-ecosystem: gomod
    directory: /
    # Trigger another immediate re-check after the Go proxy starts serving the new tag.
    schedule:
      interval: daily
    labels:
      - automerge
